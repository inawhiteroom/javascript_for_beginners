// 1. Задать температуру в градусах по Цельсию. Вывести в alert соответствующую температуру в градусах по Фаренгейту. Подсказка: расчёт идёт по формуле: Tf = (9 / 5) * Tc + 32, где Tf – температура по Фаренгейту, Tc – температура по Цельсию//
   
    var tepm_cel = prompt ("Введите температуру в градусах по Цельсию");
    var temp_far = (9 / 5) * tepm_cel + 32;
    alert (temp_far);

// 2. Объявить две переменные: admin и name. Записать в name строку "Василий"; Скопировать значение из name в admin. Вывести admin (должно вывести «Василий»).

    var admin;
    var name = "Василий";
    admin = name;
    alert (admin);

// 3. Вывести в консоль значения выражений и объяснить почему получились такие значения используя комментарии к каждому выражению:

    let result = 10 + 10 + "10"; // 10 + 10 = 20 + строка 10 = 2010
    alert (result);
    let result = 10 + "10" + 10; // склеиваются в стоку 101010
    alert (result);
    let result = 10 + 10 + +"10"; // +"строка" превращает строку в число, поэтому 30
    alert (result);
    let result = 10 / -""; // -"строка" превращает строку в отрицательное число, в данном случае в -0, деление на -0 дает отрицательную бесконечность
    alert (result);
    let result = 10 / +"2,5"; // дробные числа пишутся с ".", поэтому NaN
    alert (result);
